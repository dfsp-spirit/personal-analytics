

const SETTINGS = {
    API_BASE_URL: 'http://localhost:8000',
    ALLOW_NO_UID: true  // Whether to allow access without user ID, and map to user 'default'. Typically set to false in production.
};
